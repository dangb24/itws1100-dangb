This folder contains files for lab 6, including CSS, JavaScript/Jquery, and HTML files. 
Lab 6 is an exercise in using different JQuery methods, HTML manipulation, using CSS selectors, callback and effects. 
In problem 4b, the new list items are not under the "red" class when they are clicked because the function which adds these items 
is called after the add class "red" function, so this function is not called on the new items.  